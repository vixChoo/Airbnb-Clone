



<div class="nav_bar">
        <nav class="navbar navbar-expand-lg bg-transparent navbar-light">
				    <div class="container">
						<!-- Brand/logo -->
										
								<%= link_to image_tag("pink.png", alt: "logo",style:"width:100px;padding-top:0px;", title:"Home"), '/', class: 'navbar-brand' %>
								   <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" style="background-color: white;" aria-label="Toggle navigation">
                      <span class="navbar-toggler-icon" ></span>
                  </button>
				 		  
						<!-- Links -->
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
         		 <ul class="navbar-nav ml-auto navbar-right"  >
              
               <li class="nav-item">
								<% if signed_in? %>
									<%= link_to 'Become a Host' ,new_listing_path , class:"nav-link"%><span class="sr-only">(current)</span></li>
              							  	
                <% else %>
							  	<li class="nav-item">
                     <%= link_to 'Sign in', sign_in_path  , class:"nav-link"%></li>					
                 <% end %>

						
        <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
								<% if signed_in? %>

                  <% if current_user.avatar.present? %>
                      <%= image_tag current_user.avatar.url,width:"28",height:"28", class:"rounded-circle img-responsive"%>                              
                      <% else %>
                      
                          <%= current_user.first_name %>
                  <% end %>
                <% else %>
                    Profile
                <% end %>
        </a>
  
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
								<% if signed_in? %>

                  <%= link_to 'Profile',static_profile_path, class:"dropdown-item"%>


                   <div class="dropdown-divider"></div>
         		    	 <%= link_to 'Sign out', sign_out_path, method: :delete  , class:"dropdown-item"%>
         
                <% else %>
                <%= link_to 'Sign in', sign_in_path  , class:"dropdown-item"%>				
          <div class="dropdown-divider"></div>
                 <%= link_to "Sign in with Google", "/auth/google_oauth2", class:"dropdown-item" %>
                <% end %>
          </div>
          

         
          </li>
      </ul>
</div>
</nav>
</div>
</div>

<style>

</style>

  <%# <%= render partial:'application/sticky' %> 



<style>
  
  $dark-blue: #222;
  $green: #90feb5;
  $action-color: $green;

nav{
  box-sizing: border-box;
}

.nav_bar{
  animation: hue-rotate 10s linear infinite;
  z-index : 9999; 
}

@keyframes hue-rotate {
      from {
        -webkit-filter: hue-rotate(0);
        -moz-filter: hue-rotate(0);
        -ms-filter: hue-rotate(0);
        filter: hue-rotate(0);
      }
      to {
        -webkit-filter: hue-rotate(360deg);
        -moz-filter: hue-rotate(360deg);
        -ms-filter: hue-rotate(360deg);
        filter: hue-rotate(360deg);
      }
}

</style>